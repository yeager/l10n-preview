#!/usr/bin/env python3
import sys
import os
sys.path.insert(0, '/usr/lib/python3/dist-packages')

try:
    from l10n_preview.main import main
    main()
except ImportError:
    try:
        from l10n_preview.app import main
        main()
    except ImportError:
        print("Error: Cannot find main module for l10n-preview", file=sys.stderr)
        sys.exit(1)
